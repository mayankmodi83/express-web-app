<!DOCTYPE html>
<html>

<head>
    <title>
        {{title}}
    </title>
    <link rel="stylesheet" href="./css/styles.css" />
</head>

<body>
    {{>header}}
    <div class="main-content">
        <h3>{{errorMessage}}</h3>
    </div>
    {{>footer}}
</body>

</html>